# 现役验证记录

最后核对：2026-09-13，Windows / Node 20.19.4 / Next.js 16.3.4。

## 当前代码与门禁

- 当前 HEAD 为 `9014a17`（`Fix reflection timestamp purity lint`），本页后续结果包含尚未提交的回顾逻辑改动。
- `npm test`：22/22 通过，覆盖需求门控、最多两次模型调用、无来源降级、取消/内部截止、来源绑定、真实正向日记反例、七日周期、收藏匹配、OAuth 会话防篡改和足迹展示。
- `npm run build`：通过；每日回顾、每周回声和知乎 OAuth API 均成功编译为 Node.js 动态路由。
- `npm run lint`：通过。
- 已跟踪文件的高置信密钥扫描：未发现密钥值；`.env.local` 由 `.gitignore` 排除。

## 当前交互事实

- 底部导航只有“今天 / 回顾 / 足迹”三项；`/orchard` 沿用足迹高亮。
- 页面不监听整页左右滑动，避免与手机浏览器的边缘返回手势冲突。
- 待办右滑仅在水平位移至少 18px 且为垂直位移的 1.5 倍时开始跟随，达到 112px 才完成；长按已完成待办可恢复。
- 待办编辑弹窗通过 `document.body` portal 渲染；竖屏手机聚焦输入框时隐藏底部导航，避免软键盘把导航顶到输入区。
- 回顾请求使用 `AbortController`；服务端在 60 秒主动降级，客户端最多等待 75 秒。标签页级后台任务不随页面切换卸载，外部搜索或模型失败时仍保留日记并返回本地回顾。
- 回顾页右上角放置可选知乎登录；窄屏把「查看 / 修改日记」收短为「编辑日记」，标题左边缘不变。
- 每周回声以第一篇非演示日记为锚点，按连续七个自然日分段，独立于当日卡片生成。
- 浏览器拦截接口并延迟响应后实测：提交回顾、立即切到“今天”、响应完成后再返回“回顾”，新结果仍已写入，控制台无错误。

## 生产状态

- `https://zuoshu.caunews.cn/reflection` 于 2026-09-13 复核返回 HTTP 200，服务端标识为 EdgeOne Makers。
- 生产回顾接口用用户明确提供的日记复现：约 32.2 秒后由 EdgeOne 返回 504 HTML；DeepSeek 已在网关断开前完成计费，因此出现“有费用但页面红字”。
- 同一篇日记在改动后的本地接口用时 11.5 秒，HTTP 200，只返回 daily_highlight，知乎检索 0 次。
- **待发布**：上述 60 秒降级、90 秒 EdgeOne Node Functions 上限、跨页面后台任务、需求门控、每周回声和知乎登录仍在当前工作树，生产站尚未包含。
- EdgeOne 当前 Git 关联仍显示仓库更名前的地址；GitHub 会重定向到 `muyu-XI/zuoshu`，但最新提交没有自动触发生产部署。需在 EdgeOne 项目设置的 Git 管理中重新关联 `muyu-XI/zuoshu` 的 `main`，或手动部署 `42fd458`，再做手机真机复核。
- `edgeone.json` 和 Next 路由都声明 90 秒外层上限；每日管道在 60 秒主动降级返回。部署后需从 EdgeOne 函数日志确认配置已生效。

## 仍需人工验收

- 部署后在真实 iOS/Android 上重新提交回顾，确认在网关时限内返回。
- 软件键盘打开时，待办弹窗不被底部导航遮挡；开始作数按钮不盖住弹窗。
- 页面边缘左右滑由浏览器处理；待办卡片的刻意右滑仍可完成，纵向滚动不会误触。
- EdgeOne 部署完成后，用正向日记确认不发知乎检索，用明确未解决困惑确认最多发两条并行检索。

`output/`、`.playwright-cli/` 与用户原有的 `tmp/` 均由 gitignore 排除，不进入交付 diff。
